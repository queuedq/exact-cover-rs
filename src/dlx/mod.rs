//! A low-level API for dancing links (DLX) algorithm and its modifications.
//! 
//! If you are looking for a [`Problem`](crate::problem::Problem) solver API,
//! see the [`solver`](crate::solver) module.

pub mod dlx;
pub mod dlx_m;
pub mod callback;
